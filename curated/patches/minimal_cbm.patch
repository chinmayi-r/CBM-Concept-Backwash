diff --git a/src/datasets/__init__.py b/src/datasets/__init__.py
index b178f49..a63adca 100644
--- a/src/datasets/__init__.py
+++ b/src/datasets/__init__.py
@@ -14,5 +14,10 @@ def get_loader(dataset, **kwargs):
     elif dataset.upper()=="SPIRALS":
         from .spirals import get_spirals
         return get_spirals(**kwargs)
+    elif dataset.upper()=="FUNNYBIRDS":
+        # curated patch: FunnyBirds loader lives in curated/compat (put on
+        # sys.path by curated/train/run_mcbm.py). See curated/patches/.
+        from mcbm_funnybirds import get_funnybirds
+        return get_funnybirds(**kwargs)
     else:
         raise ValueError
\ No newline at end of file
diff --git a/src/experiments/base.py b/src/experiments/base.py
index 1ebc29d..01b51fa 100644
--- a/src/experiments/base.py
+++ b/src/experiments/base.py
@@ -43,6 +43,17 @@ class BaseExperiment:
 
     #==========Setters==========
     def _set_wandb(self):
+        # curated patch: when wandb_offline, do NOT touch wandb at all. Both
+        # login() and init() reach the network and die on network-less cluster
+        # nodes. We read every metric from stdout (Epoch N ...) and
+        # predictions/*.pth, so a no-op logger is sufficient. Online path
+        # unchanged. See curated/patches/.
+        if self.wandb_offline:
+            class _NoOpRun:
+                def log(self, *a, **k): pass
+                def finish(self, *a, **k): pass
+            self.wandb_run = _NoOpRun()
+            return
         now = datetime.now()
         wandb.login(key=self.wandb_key)
         self.wandb_run = wandb.init(
@@ -50,7 +61,7 @@ class BaseExperiment:
             config=self.cfg,
             project='mcbm',
             group=self.experiment_name,
-            mode="offline" if self.wandb_offline else "online",
+            mode="online",
             name="{experiment_name}_{config_file}_{seed}_{date}".format(
                 experiment_name=self.experiment_name,
                 config_file=self.config_file,
diff --git a/src/datasets/cub200.py b/src/datasets/cub200.py
index 3da0b1b..d16fa81 100644
--- a/src/datasets/cub200.py
+++ b/src/datasets/cub200.py
@@ -35,7 +35,9 @@ def get_cub200(
     model_kwargs = {
         'n_concepts': dataset.n_concepts,
-        'dim_y': 200,
+        # curated patch: preserve 200 for full CUB, but allow a class-filtered
+        # CUB70 training set to build a genuine 70-way task head.
+        'dim_y': max(int(d['class_label']) for d in dataset.data) + 1,
         'dim_c': 1,
         'continuous_y': False,
         'continuous_c': False,
         'ch_in': 3,
diff --git a/src/models/mcbm.py b/src/models/mcbm.py
index 3dc84a2..b970fa1 100644
--- a/src/models/mcbm.py
+++ b/src/models/mcbm.py
@@ -79,7 +79,12 @@ class MinimalConceptBottleneckModel(ConceptBottleneckModel):
         for j in range(self.n_concepts):
             #loss_z_j = self.loss_z[j](z_logits[:,self.idxs_z[j]], z[:,self.idxs_z[j]])
             #loss_z_j = ((0.8 *c[:,j] + 0.1) * ((z_logits[:,self.idxs_z[j],0]-z[:,self.idxs_z[j]])[:,0])**2).mean()
-            loss_z_j = 0.2 * (((z_logits[:,self.idxs_z[j],0]-z[:,self.idxs_z[j]])[:,0])**2).mean()
+            # Curated numerical guard: training uses autocast, so this square
+            # can otherwise overflow in float16 and poison every later update.
+            # Float32 evaluates the same MSE objective with adequate range.
+            delta_j = (z_logits[:,self.idxs_z[j],0].float()
+                       - z[:,self.idxs_z[j]].float())[:,0]
+            loss_z_j = 0.2 * (delta_j ** 2).mean()
             if loss_z is None:
                 loss_z = loss_z_j
             else:
diff --git a/src/experiments/train.py b/src/experiments/train.py
index 55a747e..54cad7f 100644
--- a/src/experiments/train.py
+++ b/src/experiments/train.py
@@ -61,6 +61,10 @@ class TrainExperiment(BaseExperiment):
             with torch.cuda.amp.autocast(enabled=True):
                 outputs = self.model(x=x, c=c, sampling=True)
                 losses = self.model.get_loss(y=y, c=c, **outputs)
+            if not torch.isfinite(losses['total']):
+                raise FloatingPointError(
+                    "non-finite training loss; aborting before optimizer.step()"
+                )
             self.optimizer.zero_grad(set_to_none=True)
             losses['total'].backward()
             self.optimizer.step()
